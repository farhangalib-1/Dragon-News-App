import { montsorret } from '@/app/layout'
import React from 'react'

const LoginPage = ({children}) => {
  return (
    <div className={`${montsorret.className}`}>
        
        <h1>This is the Login Page</h1>
    </div>
  )
}

export default LoginPage